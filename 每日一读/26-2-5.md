1. 面试题
   ```go
   func main() {
        s1 := []int{1, 2, 3}
        s2 := []int{4, 5}
        s1 = append(s1, s2)
        fmt.Println(s1)
    }
   ```
    上面的代码能否通过编译？

    答：不能。append()的第二个参数不能直接适用slice，需要用`...`操作符，将一个切片追加到另一个切片上：`append(s1,s2...)`，或者直接跟上元素：`append(s1,4,5)`。 