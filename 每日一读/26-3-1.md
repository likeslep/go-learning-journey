# 2026年3月1日

http应答包：
1. 适用net/http包创建web服务器
   1. 注册回调函数。
      `http.HandleFunc("/itcast", handler)`，参1：用户访问文件位置；参2：回调函数名

   2. 绑定服务器监听地址。
      `http.ListenAndServe("127.0.0.1:8080", nil)`

2. 回调函数
    本质：函数指针。通过地址，在某一特定位置，调用函数。
    在程序中，定义一个函数，但不显示调用。当某一条件满足时，该函数由操作系统自动调用。