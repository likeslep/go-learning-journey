# etcd

一个强一致性、高可用的分布式减脂存储系统

etcd 通过 Raft 算法确保集群中只有一个Leader节点处理写请求，其他Follower节点同步日志，从而保证强一致性。

etcd 的 MVCC 实现通过版本化存储和B树索引，提供了高效的并发控制和历史版本查询能力，同时保证了线性一致性。

强一致性：通过Raft算法保证数据的一致性和可靠性
高性能：MVCC机制实现无锁读取和高并发写入
可扩展性：模块化设计支持水平扩展
可靠性：WAL日志和定期快照确保数据持久化