# grpc

## rpc架构要点

rpc技术在架构设计上有四大部分组成，客户端、客户端stub、服务端、服务端stub
- 客户端：服务调用的发起方，也称为服务消费者
- 客户端存根：该程序运行在客户端的机器上，主要用来存储要调用的服务端地址，该程序还负责将客户端请求远端服务器程序的数据打包成数据包，通过网络发送给服务端存根程序，并接收服务端存根发送的调用结果数据包，解析返回给客户端。
- 服务端：远端计算机上运行的程序，其中有客户端要调用的方法
- 服务端存根：接收并解析客户端存根发送的请求数据包，调用服务端正真的功能方法，将服务端执行调用的结果进行数据打包发送给客户端存根。

